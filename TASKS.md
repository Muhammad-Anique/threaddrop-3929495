# ThreadDrop — Task List

> **Stack:** Next.js, TypeScript, Tailwind CSS, Supabase | **Repo:** https://github.com/Muhammad-Anique/threaddrop-3929495
> Generated by Lead Engineer | Status: In Progress

---

## Tasks

- [ ] **Task 1: Project initialization**
  Initialize the Next.js project by creating `package.json` (including next, react, react-dom, lucide-react, and @supabase/supabase-js), `tsconfig.json`, `next.config.ts`, and `.gitignore`. Commit: `chore: initialize Next.js project`.

- [ ] **Task 2: Configure Tailwind CSS**
  Create `tailwind.config.ts`, `postcss.config.js`, and `app/globals.css` with Tailwind directives and the streetwear color palette (Black, White, Washed Grey, Off-White). Commit: `chore: add Tailwind CSS config`.

- [ ] **Task 3: Set up Supabase client**
  Create `lib/supabaseClient.ts` to initialize the Supabase client using `process.env.NEXT_PUBLIC_SUPABASE_URL` and `process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY`. Create `.env.example` with these keys as placeholders. Commit: `chore: add supabase client`.

- [ ] **Task 4: Create root layout**
  Create `app/layout.tsx` to set up the global HTML structure, metadata, and font configurations for the minimalist aesthetic. Commit: `feat: add root layout`.

- [ ] **Task 5: Implement WhatsApp CTA component**
  Create `components/WhatsAppCTA.tsx`, a floating button using the link `https://wa.me/923204589040` as specified in the architecture. Commit: `feat: add WhatsApp CTA`.

- [ ] **Task 6: Build Hero component**
  Create `components/Hero.tsx` featuring a high-impact intro, "Notify Me" CTA, and streetwear-focused typography. Commit: `feat: add hero section`.

- [ ] **Task 7: Build ProductShowcase component**
  Create `components/ProductShowcase.tsx` to display a grid of T-shirt designs using `next/image` for performance optimization. Commit: `feat: add product showcase`.

- [ ] **Task 8: Build LeadForm component**
  Create `components/LeadForm.tsx` with Name and Email fields. Implement the form submission logic to insert records into the Supabase `leads` table. Commit: `feat: add lead capture form`.

- [ ] **Task 9: Assemble Main Page**
  Update `app/page.tsx` to assemble the `Hero`, `ProductShowcase`, `LeadForm`, and `WhatsAppCTA` components into a single-page scrolling experience. Commit: `feat: assemble landing page`.

- [ ] **Task 10: Create Navigation and Footer**
  Create `components/Navbar.tsx` and `components/Footer.tsx` with minimalist branding and essential links. Integrate them into the main layout or page. Commit: `feat: add nav and footer`.

- [ ] **Task 11: Write README**
  Create a comprehensive `README.md` at the repository root. Include: project title, features (lead capture, WhatsApp integration), tech stack (Next.js, Supabase), project structure tree, environment variables table, and deployment instructions for Vercel. Commit: `docs: add comprehensive README`.